// Contract IDs
export const AIXCOM_PACKAGE_ID = import.meta.env.VITE_AIXCOM_PACKAGE_ID;
export const AIXCOM_TREASURY_CAP = import.meta.env.VITE_AIXCOM_TREASURY_CAP;
export const AIXCOM_METADATA = import.meta.env.VITE_AIXCOM_METADATA;

// Swap contract constants
export const SWAP_POOL_ID = import.meta.env.VITE_SWAP_POOL_ID;

// Token Configuration
export const TOKEN_DECIMALS = Number(import.meta.env.VITE_TOKEN_DECIMALS);
export const TOKENS_PER_SUI = Number(import.meta.env.VITE_TOKENS_PER_SUI);

// Wallet
export const WALLET_ADDRESS = import.meta.env.VITE_WALLET_ADDRESS;
